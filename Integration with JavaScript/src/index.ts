// npm i --save-dev @types/lodash --> to install declaration file for lodash from definitely typed github repository
// npm i chalk --> to install chalk which provide own declaration file no need fo rit to install from definitely typed library



import * as _ from 'lodash';

_.clone([1, 2, 3])